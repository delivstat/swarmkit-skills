apiVersion: swarmkit/v1
kind: Skill
metadata:
  id: git-log
  name: Git Log
  description: Returns commit history. History answers *why* a line looks the way it does. An agent without
    it re-litigates decisions already made.
category: capability
implementation:
  type: mcp_tool
  server: git
  tool: git_log
iam:
  required_scopes:
  - workspace:read
provenance:
  authored_by: human
  version: 1.0.0
  requires_runtime: '>=1.199.0'
  registry: swarmkit-skills
