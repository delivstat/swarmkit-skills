apiVersion: swarmkit/v1
kind: Skill
metadata:
  id: excel-write-data
  name: Excel Write Data
  description: Writes values into a cell range. The point of a report-producing agent. Also the call that
    overwrites somebody's data.
category: capability
implementation:
  type: mcp_tool
  server: excel
  tool: write_data_to_excel
iam:
  required_scopes:
  - workspace:write
provenance:
  authored_by: human
  version: 1.0.0
  requires_runtime: '>=1.199.0'
  registry: swarmkit-skills
