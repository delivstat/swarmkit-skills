apiVersion: swarmkit/v1
kind: Skill
metadata:
  id: convert-time
  name: Convert Time
  description: 'Converts a time between timezones. Timezone arithmetic is a classic quiet-failure: plausible,
    wrong, and nobody checks.'
category: capability
implementation:
  type: mcp_tool
  server: time
  tool: convert_time
iam:
  required_scopes:
  - workspace:read
provenance:
  authored_by: human
  version: 1.0.0
  requires_runtime: '>=1.199.0'
  registry: swarmkit-skills
